# AI 聚合助手技术验收方案

> 版本：v1.0  
> 日期：2026-08-08  
> 适用范围：`TECHNICAL_PLAN.md` 的 Phase 0–2（可公开试用的 MVP）  
> 目标：用可重复测试证明系统功能完整、数据可信、权限隔离、性能达标，并且可部署、可回滚、可观测。

## 1. 通过条件

版本只有同时满足下列条件，才能判定“技术验收通过”：

1. 所有 G0（发布阻断）和 G1（核心质量）指标全部通过。
2. P0、P1 缺陷均为 0。
3. 同一候选版本的全量自动化测试连续运行 3 次全部通过；核心 E2E 最近 10 次 CI 无偶发失败。
4. Preview 完成全量验收；Production 完成冒烟、监控和回滚演练。
5. 证据可追溯到同一个 Git commit、migration 版本和 Vercel deployment ID。

最多允许遗留 5 个 P2 缺陷，但每个必须有负责人、修复日期和产品负责人书面接受；不得影响核心聊天、数据、安全及核心无障碍路径。

## 2. 范围

### 2.1 本期验收

- 注册、登录、退出和受保护路由
- 工作区与 owner/admin/member 权限
- AI Gateway 多模型聊天
- 一个 OpenAI-compatible 自定义 Provider
- 模型连接测试、同步、启停和选择
- 流式输出、停止、重试、编辑重发和分支
- 会话保存、恢复、搜索、重命名、删除和导出
- PNG/JPEG/PDF/TXT/Markdown 附件
- 助手角色与系统提示词
- token、费用、延迟、错误、限流、预算和 fallback
- Vercel Preview/Production 部署、监控和回滚
- 桌面端、移动端、暗色模式和键盘核心操作

### 2.2 本期不阻断发布

长期记忆、知识库/RAG、联网及外部写操作工具、BYOK、支付、MCP 和 Agent 工作流不在 MVP 验收范围。它们上线前必须增加专项验收。

## 3. 测试基线

### 3.1 环境

| 环境 | 用途 | 数据要求 |
|---|---|---|
| Local/CI | 单元、集成、契约、安全测试 | 临时数据库、Mock Provider、无生产密钥 |
| Vercel Preview | 全量 E2E、真实 Provider 冒烟、性能测试 | 独立 Clerk、Neon 分支、Blob 和 Redis |
| Production | 发布后冒烟和监控确认 | 仅专用验收账号和最小测试数据 |

Preview 与 Production 的数据库、登录密钥、AI Key、Redis 和 Blob 必须完全分离；任一资源复用即 G0 不通过。

### 3.2 固定数据和矩阵

- Workspace A、B；每个各有 owner、admin、member。
- 1 个无权限账号和 1 个未登录会话。
- 3 家不同 Gateway Provider 的文本模型，外加 1 个兼容 Provider。
- 200 条模型 fixture，覆盖新增、修改、下架、重复及非法数据。
- 100 轮会话 fixture，覆盖文本、代码、附件、错误和分支。
- 合法与非法附件各 20 个、XSS payload 20 个、SSRF 目标 12 个。
- Chrome、Edge、Safari 最新两个主版本；当前 iOS Safari、Android Chrome。
- 视口：1440×900、1280×720、390×844、360×800。

真实模型输出具有非确定性，自动验收只判断协议、流式传输、持久化、错误和用量；固定输出正确性由 Mock Provider 验证。

## 4. 总体指标

| 领域 | 指标 | 门槛 |
|---|---|---|
| 功能 | 核心 E2E 通过率 | 100%（G0） |
| 权限 | 越权数据泄露 | 0 次（G0） |
| 数据 | 消息丢失、重复、错序 | 0 次（G0） |
| 安全 | 未接受的 Critical/High 漏洞 | 0 个（G0） |
| 接入 | 3 家 Gateway Provider + 1 个兼容 Provider | 全部可用（G0） |
| 稳定性 | Mock Provider 压测成功率 | ≥99.5%（G1） |
| 性能 | 平台流式转发额外延迟 p95 | ≤300 ms（G1） |
| 前端 | Lighthouse Performance | 移动 ≥85，桌面 ≥95（G1） |
| 无障碍 | axe 严重/致命问题 | 0 个（G1） |
| 部署 | Production 回滚时间 | ≤10 分钟（G0） |
| 可观测 | AI 请求关联 ID 覆盖率 | 100%（G1） |

## 5. 功能验收

### 5.1 账号与会话

| ID | 验收项 | 方法与量化标准 | 等级 |
|---|---|---|---|
| AUTH-01 | 注册、登录、退出 | 每种开启方式各执行 5 次，全部成功；退出后受保护资源不可访问 | G0 |
| AUTH-02 | 未登录保护 | 遍历全部受保护页面/API；页面跳登录，API 100% 返回 401 且无业务数据 | G0 |
| AUTH-03 | 会话过期 | token 过期后读写均被拒绝，前端 3 秒内提示重新登录 | G1 |
| AUTH-04 | 多标签页退出 | 3 个标签页同时登录；退出后 10 秒内均不能继续写入 | G1 |

### 5.2 聊天

| ID | 验收项 | 方法与量化标准 | 等级 |
|---|---|---|---|
| CHAT-01 | 基本聊天 | 三家 Gateway Provider 各 10 次；30 次均流式显示、正常结束并入库 | G0 |
| CHAT-02 | 停止 | 首段后停止 20 次；均在 2 秒内停止 UI 更新，状态为 `stopped` 且可重试 | G1 |
| CHAT-03 | 重新生成 | 执行 20 次；每次产生独立分支，原回答不覆盖，父子关系 100% 正确 | G1 |
| CHAT-04 | 编辑重发 | 编辑历史消息 20 次；均产生新分支，编辑点以前历史不变 | G1 |
| CHAT-05 | 切换模型 | 单会话轮换 4 个模型共 20 轮；模型快照准确率 100% | G0 |
| CHAT-06 | 刷新恢复 | 流结束后刷新 50 次；内容、顺序、状态、附件和模型快照 100% 一致 | G0 |
| CHAT-07 | 中断恢复 | 模拟断网/关连接 100 次；无损坏、无永久 `streaming` 状态 | G0 |
| CHAT-08 | 幂等提交 | 相同 idempotency key 重放 100 次；每组只有 1 条用户消息和 1 个 generation | G0 |
| CHAT-09 | 错误归一 | 401、429、5xx、超时、断流各 10 次；错误码准确率 100%，无敏感信息 | G0 |
| CHAT-10 | Fallback | 主 Provider 失败 20 次；配置时全部切换并记录实际 Provider，否则明确失败 | G1 |

### 5.3 会话与附件

| ID | 验收项 | 方法与量化标准 | 等级 |
|---|---|---|---|
| CONV-01 | 列表分页 | 250 个会话完整翻页，无重复遗漏，排序准确率 100% | G1 |
| CONV-02 | 搜索 | 50 个已知关键词命中率 100%，其他工作区结果为 0 | G1 |
| CONV-03 | 重命名 | 连续修改、刷新 20 次，列表与详情 100% 一致 | G1 |
| CONV-04 | 导出 | 100 轮含分支会话，文本、角色、时间、模型、附件元数据完整率 100% | G1 |
| CONV-05 | 删除 | 20 个会话立即不可见；DB 记录 60 秒内删除；Blob 24 小时内删除或任务可追踪 | G0 |
| FILE-01 | 合法上传 | 5 类文件各 4 个，20 个全部成功，MIME、大小、归属、hash 正确 | G1 |
| FILE-02 | 非法上传 | 超 20 MB、伪 MIME、双扩展等 20 个全部拒绝，孤儿 DB/Blob 为 0 | G0 |
| FILE-03 | 文件隔离 | B 访问 A 的 20 个附件，全部返回 403/404且不泄露内容 | G0 |
| FILE-04 | 能力过滤 | 纯文本模型接收图片/文件各 10 次，均在调用上游前阻止 | G1 |

## 6. 模型与管理后台

| ID | 验收项 | 方法与量化标准 | 等级 |
|---|---|---|---|
| MODEL-01 | Gateway 覆盖 | 三家 Provider 各一款模型，各连续成功聊天 10 次 | G0 |
| MODEL-02 | 兼容接口 | 完成配置、连接测试、同步、前端选择、流式聊天，全链路通过 | G0 |
| MODEL-03 | 错误密钥 | 测试 10 次，均返回 `PROVIDER_AUTH_FAILED`，响应和日志无密钥 | G0 |
| MODEL-04 | 同步准确 | 导入 200 条 fixture，新增/更新/下架 100% 一致，非法项有原因 | G1 |
| MODEL-05 | 同步幂等 | 相同目录连续同步 3 次，后两次业务变更为 0，重复记录为 0 | G0 |
| MODEL-06 | 禁用保护 | 禁用 20 个模型后再同步，20 个全部保持禁用 | G0 |
| MODEL-07 | 可见时效 | 手工刷新后 60 秒内出现在后台；启用后 10 秒内出现在选择器 | G1 |
| MODEL-08 | 定时同步 | 连续观察 3 个周期，均按时完成且无并发重复任务 | G1 |
| MODEL-09 | 能力映射 | text/image/file/tools fixture 映射准确率 100% | G0 |
| MODEL-10 | 自定义地址安全 | 本机、内网、metadata、IPv6、DNS 样例 12 个全部在请求前拦截 | G0 |

## 7. 数据与上下文

| ID | 验收项 | 方法与量化标准 | 等级 |
|---|---|---|---|
| DATA-01 | 历史一致性 | 100 轮会话读取、刷新、导出的规范化 hash 完全一致 | G0 |
| DATA-02 | 事务完整性 | 在消息写入各阶段注入故障 50 次；无孤儿、重复或假完成记录 | G0 |
| DATA-03 | 并发写入 | 同一会话并发 20 请求，重复 10 组；无丢失且排序字段唯一 | G0 |
| DATA-04 | 上下文预算 | 10 种边界长度各 10 次；输入 token + 预留输出不超过窗口 90% | G0 |
| DATA-05 | 摘要触发 | 20 个超阈值会话全部触发摘要/裁剪，原历史完整可查 | G1 |
| DATA-06 | 模型快照 | 模型改名、改价、下架后，50 条旧消息仍显示调用时快照 | G1 |
| DATA-07 | 用量覆盖 | Mock 固定 token 的 1,000 次请求，usage event 覆盖率 100% | G0 |
| DATA-08 | 费用准确 | 50 组价格 fixture 使用 Decimal 计算，与基准偏差 ≤0.1% | G1 |
| DATA-09 | 备份恢复 | 隔离环境恢复：RPO ≤24 小时、RTO ≤4 小时，核心表抽样 hash 一致 | G0 |

## 8. 权限与安全

使用 2 个工作区、3 种角色，对会话、附件、助手、Provider、模型、用量六类资源执行读/写/删，不少于 108 个权限用例：允许项成功率 100%，拒绝项拦截率 100%，跨工作区响应不含目标资源字段。

| ID | 验收项 | 方法与量化标准 | 等级 |
|---|---|---|---|
| SEC-01 | 密钥加密 | DB、构建产物、网络响应、最近 24 小时日志中明文 Key 为 0 | G0 |
| SEC-02 | 依赖审计 | Production dependencies 未接受的 Critical/High 为 0 | G0 |
| SEC-03 | 静态扫描 | 候选 commit 的 SAST/CodeQL 未处理 Critical/High 为 0 | G0 |
| SEC-04 | XSS | Markdown、文件名、标题共 20 个 payload，脚本执行为 0 | G0 |
| SEC-05 | CSRF | 所有 cookie 鉴权写接口跨站写入成功数为 0 | G0 |
| SEC-06 | 限流 | 默认 20 次/用户/分钟；第 21 次返回 429，窗口后恢复 | G1 |
| SEC-07 | 日预算 | 并发越界 20 次，超预算成功数为 0 | G0 |
| SEC-08 | 错误脱敏 | 注入 30 类异常，堆栈、SQL、密钥、内部路径泄露为 0 | G0 |
| SEC-09 | 导出/删除授权 | 跨用户导出和删除各 20 次，40 次全部失败且数据不变 | G0 |

## 9. 性能与容量

第三方模型推理时间与本应用平台开销分开统计。性能测试同时报告冷启动和热请求。

### 9.1 前端性能

在 Preview 的 Production 构建上，每个页面测试 5 次取中位数：

| 指标 | 移动端 | 桌面端 |
|---|---:|---:|
| Lighthouse Performance | ≥85 | ≥95 |
| Lighthouse Accessibility | ≥95 | ≥95 |
| CLS | ≤0.10 | ≤0.10 |
| 聊天页首屏压缩后 JS | ≤300 KB | ≤300 KB |

发布后样本达到 1,000 次页面访问时，7 天滚动 p75 目标：LCP ≤2.5 秒、INP ≤200 ms、CLS ≤0.1。首次发布时样本不足不阻断，但数据达到门槛后不达标必须进入性能整改。

### 9.2 API 和流式性能

使用固定流速、无外网延迟的 Mock Provider：

| ID | 指标与负载 | 通过标准 | 等级 |
|---|---|---|---|
| PERF-01 | 平台首段额外延迟；1,000 请求、20 并发 | p95 ≤300 ms，p99 ≤500 ms | G1 |
| PERF-02 | Provider chunk 到浏览器可见；同上 | 额外延迟 p95 ≤300 ms | G1 |
| PERF-03 | 250 会话列表；1,000 请求 | p95 ≤300 ms，p99 ≤600 ms | G1 |
| PERF-04 | 流结束到持久化完成；1,000 次 | p95 ≤1 秒，p99 ≤2 秒 | G1 |
| PERF-05 | 20 并发持续 10 分钟 | 成功率 ≥99.5%，应用 5xx ≤0.5% | G1 |
| PERF-06 | 资源稳定性；同 PERF-05 | 无持续泄漏趋势，结束 2 分钟内回落至基线 ±20% | G1 |

真实 Provider 每个选定模型另做 10 次冒烟；首 token 时间只记录基线，不与平台性能混算，但必须在产品超时内完成或返回明确超时错误。

## 10. 稳定性与容灾

| ID | 验收项 | 方法与量化标准 | 等级 |
|---|---|---|---|
| REL-01 | Redis 故障 | 中断后读写各 20 次；只读可降级，写入不得绕过预算 | G0 |
| REL-02 | DB 短暂故障 | 注入 5–30 秒故障 10 次；无假成功，恢复后无需人工修数据 | G0 |
| REL-03 | Provider 超时 | 注入 30 次；全部在配置时限内结束，无永久生成状态 | G0 |
| REL-04 | Cron 重入 | 并发触发 10 组；单 Provider 同时最多 1 个有效任务 | G1 |
| REL-05 | 冷启动 | 20 次冷请求全部成功，无初始化竞态 | G1 |
| REL-06 | 依赖恢复 | 依赖恢复后 5 分钟内无需部署即可通过核心冒烟 | G1 |

上线后的应用层月可用性目标为 ≥99.9%，不含公告维护及经证实的第三方 Provider 故障。应用 5xx、无响应和异常断流计为不可用；401、403、429 等受控业务拒绝不计为不可用。

## 11. 可用性与无障碍

| ID | 验收项 | 量化标准 | 等级 |
|---|---|---|---|
| UX-01 | 响应式 | 4 个固定视口无横向溢出，发送/停止/切模/历史均可操作 | G1 |
| UX-02 | 键盘 | 不使用鼠标完成登录、新建、选模型、发送、停止、退出 | G1 |
| UX-03 | 焦点 | 弹窗、发送、错误后的 20 个焦点用例全部通过 | G1 |
| UX-04 | axe | 严重/致命问题为 0；其他问题不超过 5 个且有豁免 | G1 |
| UX-05 | 对比度 | 明暗主题核心组件检查通过率 100% | G1 |
| UX-06 | 状态反馈 | 加载、空、断网、限流、无权、Provider 失败均有下一步 | G1 |
| UX-07 | 长内容 | 10 万字符消息、长代码、100 轮会话可浏览且不崩溃 | G1 |

## 12. 可观测性与成本

| ID | 验收项 | 方法与量化标准 | 等级 |
|---|---|---|---|
| OBS-01 | 请求关联 | 抽样 200 次；响应、应用日志、usage event 的 request ID 覆盖 100% | G0 |
| OBS-02 | 错误记录 | 注入 30 类错误，均记录错误码、Provider、模型、耗时和 ID | G1 |
| OBS-03 | 指标面板 | 可查请求量、成功率、p50/p95/p99、首 token、token、费用、429、5xx | G1 |
| OBS-04 | 错误告警 | 5 分钟窗口制造 >5% 应用错误率，5 分钟内送达 | G0 |
| OBS-05 | 费用告警 | 制造 2 倍日常速率或预算阈值，5 分钟内告警；硬预算后拒绝 | G0 |
| OBS-06 | 管理审计 | 20 次 Provider/模型操作，操作者、时间、前后值、结果完整率 100% | G1 |

聊天正文、附件正文和完整系统提示词默认不得进入普通日志。临时调试必须显式开启、脱敏并设置短期保留。

## 13. CI/CD 与发布

### 13.1 合并门禁

每个 PR 必须执行并通过：

```text
format/check → lint → typecheck → unit → integration
→ security scan → build → Playwright core E2E
```

- 所有任务退出码为 0，不得跳过 G0/G1 用例。
- 候选 commit 与 Preview deployment commit 完全一致。
- 全仓行覆盖率 ≥75%、分支覆盖率 ≥70%。
- `server/ai`、`server/security`、权限和费用模块行/分支覆盖率均 ≥85%。
- 覆盖率下降超过 2 个百分点时阻断，生成文件或书面豁免除外。

### 13.2 部署与回滚

| ID | 验收项 | 量化标准 | 等级 |
|---|---|---|---|
| DEP-01 | Preview 隔离 | 环境变量和资源检查无任何 Production 连接 | G0 |
| DEP-02 | 数据库迁移 | 在生产数据量副本升级成功并通过向后兼容检查 | G0 |
| DEP-03 | Production 冒烟 | 登录、模型列表、发送、保存、刷新、退出在 10 分钟内全过 | G0 |
| DEP-04 | 回滚 | 决定回滚后 ≤10 分钟恢复，核心冒烟全过 | G0 |
| DEP-05 | 迁移兼容 | 应用回滚后数据错误和不可读记录为 0 | G0 |
| DEP-06 | 发布审计 | 版本、commit、migration、操作者、时间字段完整率 100% | G1 |

数据库 migration 采用 expand/contract：先增加兼容结构，稳定后再删除旧结构。破坏性 migration 不得依赖应用回滚恢复数据库。

## 14. 缺陷等级

| 等级 | 定义 | 示例 | 发布要求 |
|---|---|---|---|
| P0 | 数据/密钥泄露、不可恢复、全站不可用 | 跨工作区读取、Key 明文 | 必须为 0 |
| P1 | 核心功能不可用或大量用户受影响 | 无法登录/聊天、消息丢失 | 必须为 0 |
| P2 | 非核心异常，有绕行且不损害数据安全 | 次要浏览器样式问题 | ≤5，书面接受 |
| P3 | 文案、轻微视觉或优化建议 | 间距不一致 | 不阻断 |

自动化失败不能仅凭“重跑通过”关闭；必须定位为代码、环境、数据或测试问题，并留下证据。

## 15. 执行顺序

1. 冻结候选 commit 和 migration，生成 Preview。
2. 执行静态检查、单元、集成、契约和安全测试。
3. 执行权限矩阵、数据一致性和备份恢复。
4. 执行 Mock Provider 性能、故障注入和真实 Provider 冒烟。
5. 执行浏览器矩阵、移动端和无障碍检查。
6. 汇总缺陷；修复后从受影响层向上回归。
7. 同一候选版本连续完成 3 次全量通过。
8. 发布 Production，10 分钟内完成核心冒烟。
9. 完成告警和回滚演练；观察 24 小时无 P0/P1 后签署结论。

## 16. 证据与签署

每次验收保存：

```text
Release version / Git commit
Preview 与 Production deployment ID/URL
Database migration version
CI、E2E、安全、性能、恢复报告链接
回滚耗时
P2/P3 遗留问题及负责人
```

签署角色：开发负责人、测试/验收负责人、产品负责人、运维/发布负责人。

最终结论模板：

```text
结论：通过 / 有条件通过 / 不通过
G0：通过数 / 总数
G1：通过数 / 总数
P0：0
P1：0
P2：数量及豁免链接
观察期：开始时间 - 结束时间
签署人及时间：
```

“有条件通过”只适用于允许遗留的 P2；任一 P0、P1 或 G0/G1 未通过时，结论必须为“不通过”。
